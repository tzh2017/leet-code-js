"use strict";
var chai = require("chai");
var expect = chai.expect;

var main = require("../lib/0136-single-number");

describe(main.name, function () {
  it("calc", function () {
    expect(main.func).equal(1);
  });
});
